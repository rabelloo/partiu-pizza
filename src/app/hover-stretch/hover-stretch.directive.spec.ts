import { HoverStretchDirective } from './hover-stretch.directive';

describe('HoverStretchDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverStretchDirective();
    expect(directive).toBeTruthy();
  });
});
